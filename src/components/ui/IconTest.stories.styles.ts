import type { CSSProperties } from 'react';
import { lightTheme } from '@/themes/themes';

export const iconTestPageStoriesStyles = {
  centeredRoot: {
    padding: '20px',
    textAlign: 'center',
  } satisfies CSSProperties,
};

export const iconTestStoryColors = {
  customIconColor: lightTheme.colors.danger,
};
