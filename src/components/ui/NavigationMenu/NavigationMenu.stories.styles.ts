import type { CSSProperties } from 'react';

export const navigationMenuStoriesStyles = {
  storyContainer: {
    maxWidth: 280,
  } satisfies CSSProperties,
  toggleVisibilityButton: {
    font: 'inherit',
    cursor: 'pointer',
  } satisfies CSSProperties,
};
