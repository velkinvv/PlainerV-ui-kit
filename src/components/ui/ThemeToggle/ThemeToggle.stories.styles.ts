import type { CSSProperties } from 'react';

export const themeToggleStoriesStyles = {
  withBackgroundContainer: {
    padding: '20px',
    background: 'var(--background)',
    borderRadius: '8px',
    display: 'flex',
    alignItems: 'center',
    gap: '16px',
  } satisfies CSSProperties,
};
