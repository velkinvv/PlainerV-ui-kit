import type { CSSProperties } from 'react';

export const tableStoriesStyles = {
  footerCellPadding: {
    padding: '16px',
  } satisfies CSSProperties,
  stripedSizesContainer: {
    display: 'flex',
    flexDirection: 'column',
    gap: 24,
  } satisfies CSSProperties,
};
