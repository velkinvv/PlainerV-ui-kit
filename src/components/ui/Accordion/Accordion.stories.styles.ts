import type { CSSProperties } from 'react';

export const accordionStoriesStyles = {
  controlledContainer: {
    width: '400px',
  } satisfies CSSProperties,
  controlledCaption: {
    marginBottom: '16px',
  } satisfies CSSProperties,
};
