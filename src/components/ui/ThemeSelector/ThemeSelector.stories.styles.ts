import type { CSSProperties } from 'react';

/**
 * Стили сторис {@link ThemeSelector}.
 */
export const themeSelectorStoriesStyles = {
  panel: {
    display: 'flex',
    flexDirection: 'column',
    gap: '16px',
    minWidth: '280px',
  } satisfies CSSProperties,
};
