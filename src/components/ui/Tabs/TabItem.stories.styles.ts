import type { CSSProperties } from 'react';

export const tabItemStoriesStyles = {
  contentPadding16: {
    padding: '16px',
  } satisfies CSSProperties,
  iconLabelRow: {
    display: 'flex',
    alignItems: 'center',
    gap: '8px',
  } satisfies CSSProperties,
};
