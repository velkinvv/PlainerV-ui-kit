import type { CSSProperties } from 'react';

export const floatingMenuStoriesStyles = {
  decoratorContainer: {
    minHeight: 400,
    position: 'relative',
  } satisfies CSSProperties,
  placementsContainer: {
    padding: 24,
  } satisfies CSSProperties,
  placementsLabelRow: {
    marginBottom: 16,
  } satisfies CSSProperties,
};
